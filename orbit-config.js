/*
  Local Orbit configuration.

  Paste your own key after apiKey. Do not upload this file to GitHub,
  send it to anyone, or use it on a public website.

  A browser-only key is suitable only for a private, local experiment.
  For a real public website, use a secure server or Cloudflare Worker instead.
*/
window.ORBIT_API_CONFIG = {
  endpoint: "https://api.openai.com/v1",
  apiKey: "PASTE_YOUR_OPENAI_API_KEY_HERE"
};
